spring:
  cloud:
    config:
      uri:
       - http://${cloud.config.host:localhost}:${cloud.config.port:8001}
  application:
    name: ${springboot.app.name:account-management-service}



management:
  security:
    enabled: false    